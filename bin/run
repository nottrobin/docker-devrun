#!/bin/sh

set -e

# If dependencies have changed since last build, build the app image again
[[ ${DEPENDENCIES_MODIFIED} -gt $(cat ${LAST_BUILD_FILE} || echo "0") ]] && build

# Update database with last migrations
migrate
docker-compose up web db
